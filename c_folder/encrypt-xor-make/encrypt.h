void encrypt(char* message);

/*A header file contains the function declarations (prototypes), macros, constants, and struct 
definitions, but not the actual implementation of the function. The actual function implementation 
would still go in a corresponding .c file.*/